environment = "dev"
aws_region  = "us-gov-west-1"

# S3 Buckets
raw_bucket_name = "entergy-govdatacore-raw-dev"

# DMS Configuration
 
glue_job_name             = "dms_data_quality_load"
log_retention_days        = 30

# IAM Roles (must be created outside Terraform)
crawler_execution_role_arn = "arn:aws-us-gov:iam::331875467123:role/glue-execution-role"

# DMS Configuration
dms_replication_instance_id = "dms-replication-instance-dev"
