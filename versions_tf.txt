terraform {
  backend "s3" {
    # Backend configuration will be provided via -backend-config flags
  }
  
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"  # Use version 5.x which supports S3 as DMS target
    }
  }
}